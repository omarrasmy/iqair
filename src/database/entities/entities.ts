import { AirQualities } from "src/air_qualities/db/air_quality.entity";
import { Cities } from "src/cities/db/city.entity";

const entities = {
  air_qualities: AirQualities,
  cities: Cities,
};
export default entities;
